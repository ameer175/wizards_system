import classes from './home.module.css';

const Home = () => {
    return (
        <div className={classes.hero}>
            <div>some logo</div>
            
        </div>
    );
};

export default Home;
